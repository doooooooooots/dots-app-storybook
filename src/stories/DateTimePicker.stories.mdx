import { Canvas, Meta, Story } from "@storybook/addon-docs";

import DateTimePicker from "../components/date-time-picker/DateTimePicker";

<Meta
  title="Design System/DateTimePicker"
  component={DateTimePicker}
  argTypes={{
    variant: {
      options: ["text", "contained", "outlined"],
      control: { type: "radio" },
    },
  }}
/>

# Button

### Controls

blablabla text

<Canvas>
  <Story name="controls">{Template.bind({})}</Story>
</Canvas>

### Initials

blablabla text

<Story name="initials">
  <div>
    <DateTimePicker label="read-only" readOnly />
  </div>
</Story>

## Standard

blablabla text

export const Template = (args) => (
  <DateTimePicker {...args} label="read-only" readOnly />
);

<Story name="standard">{Template.bind({})}</Story>
