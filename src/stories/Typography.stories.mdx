import { Canvas, Meta, Story } from "@storybook/addon-docs";

import Typography from "../components/typography/Typography";

<Meta
  title="Design System/Typography"
  component={Typography}
  argTypes={{
    variant: {
      options: ["h1", "h2", "h3", "h4", "h5", "h6", "span", "p"],
      control: { type: "radio" },
    },
  }}
/>

# Button

### Controls

blablabla text

<Canvas>
  <Story name="controls">{Template.bind({})}</Story>
</Canvas>

### Initials

blablabla text

<Story name="initials">
  <div>
    <Typography>Typography</Typography>
  </div>
</Story>

## Standard

blablabla text

export const Template = (args) => <Typography {...args}>Typography</Typography>;

<Story name="standard">{Template.bind({})}</Story>
