import { Canvas, Meta, Story } from "@storybook/addon-docs";

import Card from "../components/card/Card";

<Meta
  title="Design System/Card"
  component={Card}
  argTypes={{
    variant: {
      options: ["text", "contained", "outlined"],
      control: { type: "radio" },
    },
  }}
/>

# Button

### Controls

blablabla text

<Canvas>
  <Story name="controls">{Template.bind({})}</Story>
</Canvas>

### Initials

blablabla text

<Story name="initials">
  <div>
    <Card variant="outlined">Test</Card>
  </div>
</Story>

## Standard

blablabla text

export const Template = (args) => <Card {...args}>Test</Card>;

<Story name="standard">{Template.bind({})}</Story>
